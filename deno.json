{
  "tasks": {
    "ci": "deno fmt --check && deno lint && deno test",
    "fix": "deno fmt && deno lint && deno test && deno task run-example",
    "run-example": "deno run --allow-read=example --allow-write=example example/gen.ts"
  }
}
